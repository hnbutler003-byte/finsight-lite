<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Finsight Lite — Money Skills for the Caribbean's Next Generation</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Baloo+2:wght@500;600;700;800&family=Nunito:wght@400;600;700;800&display=swap');

  :root {
    --primary: 262 83% 58%;
    --primary-deep: 262 72% 26%;
    --secondary: 32 95% 60%;
    --teal: 190 75% 48%;
    --teal-deep: 190 72% 20%;
    --coral: 15 90% 65%;
    --ink: 262 40% 18%;

    --grad-1: hsl(262, 72%, 26%);
    --grad-2: hsl(288, 68%, 22%);
    --grad-3: hsl(190, 72%, 20%);
    --grad-4: hsl(262, 78%, 18%);

    --radius-glass: 28px;
    --radius-inset: 18px;
    --radius-badge: 999px;

    --font-display: 'Baloo 2', cursive;
    --font-body: 'Nunito', sans-serif;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: var(--font-body);
    color: white;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }

  h1, h2, h3, h4 {
    font-family: var(--font-display);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.01em;
  }

  a { color: inherit; }

  .caribbean-bg {
    background: linear-gradient(135deg, var(--grad-1) 0%, var(--grad-2) 30%, var(--grad-3) 65%, var(--grad-4) 100%);
    background-size: 400% 400%;
    animation: drift 30s ease infinite;
    position: relative;
    overflow: hidden;
  }

  .caribbean-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 60% at 20% 25%, hsl(190 80% 55% / 0.14) 0%, transparent 60%),
      radial-gradient(ellipse 60% 50% at 85% 75%, hsl(262 83% 65% / 0.12) 0%, transparent 60%);
    pointer-events: none;
  }

  @keyframes drift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  .section {
    position: relative;
    padding: 90px 24px;
    max-width: 1080px;
    margin: 0 auto;
  }

  .section-narrow { max-width: 760px; }

  .eyebrow {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 13px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: hsl(var(--secondary));
    margin-bottom: 14px;
    display: inline-block;
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.10);
    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: var(--radius-glass);
    box-shadow: 0 8px 32px rgba(20, 10, 40, 0.25), inset 0 1px 0 rgba(255,255,255,0.14);
    padding: 36px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .glass-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 48px rgba(20, 10, 40, 0.32), inset 0 1px 0 rgba(255,255,255,0.18);
  }

  .glass-card-light {
    background: rgba(255, 255, 255, 0.95);
    color: hsl(var(--ink));
    border-radius: var(--radius-glass);
    box-shadow: 0 8px 32px rgba(20, 10, 40, 0.22);
    padding: 36px;
  }

  .glass-card-light h3, .glass-card-light h4 { color: hsl(var(--primary)); }

  /* ── HERO ───────────────────────────────────────────── */
  .hero {
    text-align: center;
    padding-top: 110px;
    padding-bottom: 70px;
  }

  .hero-logo {
    display: inline-flex;
    align-items: flex-end;
    gap: 10px;
    margin-bottom: 36px;
    animation: pop-in 0.6s cubic-bezier(0.34,1.56,0.64,1) both;
  }

  .hero-bars { display: flex; align-items: flex-end; gap: 6px; }
  .bar { border-radius: 999px; }
  .bar1 { width: 14px; height: 22px; background: hsl(var(--primary)); }
  .bar2 { width: 14px; height: 36px; background: hsl(var(--teal)); }
  .bar3 { width: 14px; height: 54px; background: linear-gradient(180deg, #EF7C5C, hsl(var(--primary))); border-radius: 999px 999px 0 0; position: relative; }
  .bar3::before {
    content: '';
    position: absolute;
    top: -16px; left: 50%;
    transform: translateX(-50%);
    width: 16px; height: 16px;
    border-radius: 50%;
    background: hsl(var(--secondary));
  }
  .hero-wordmark { font-family: var(--font-display); font-weight: 800; font-size: 28px; color: white; text-align: left; line-height: 1; }
  .hero-wordmark span { display: block; font-size: 22px; color: hsl(var(--secondary)); }

  .hero h1 {
    font-size: clamp(34px, 5.5vw, 58px);
    max-width: 820px;
    margin: 0 auto 22px;
    animation: pop-in 0.7s 0.1s cubic-bezier(0.34,1.56,0.64,1) both;
  }

  .hero p {
    font-size: 18px;
    color: rgba(255,255,255,0.82);
    max-width: 560px;
    margin: 0 auto 36px;
    animation: pop-in 0.7s 0.2s cubic-bezier(0.34,1.56,0.64,1) both;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: linear-gradient(135deg, hsl(var(--primary)), hsl(262 83% 48%));
    color: white;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 16px;
    padding: 16px 34px;
    border-radius: 999px;
    text-decoration: none;
    box-shadow: 0 4px 20px hsl(var(--primary) / 0.45);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    animation: pop-in 0.7s 0.3s cubic-bezier(0.34,1.56,0.64,1) both;
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px hsl(var(--primary) / 0.55);
  }

  .btn-primary svg { transition: transform 0.2s ease; }
  .btn-primary:hover svg { transform: translateX(3px); }

  @keyframes pop-in {
    0% { opacity: 0; transform: scale(0.92) translateY(14px); }
    100% { opacity: 1; transform: scale(1) translateY(0); }
  }

  /* ── REVEAL ON SCROLL ───────────────────────────────── */
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── TRUST STRIP ────────────────────────────────────── */
  .trust-strip {
    display: flex;
    align-items: center;
    gap: 16px;
    max-width: 600px;
    margin: 0 auto;
  }

  .trust-icon {
    flex-shrink: 0;
    width: 46px; height: 46px;
    border-radius: 14px;
    background: rgba(255,255,255,0.15);
    display: flex; align-items: center; justify-content: center;
  }

  .trust-text { flex: 1; }
  .trust-title { font-family: var(--font-display); font-weight: 700; font-size: 15px; color: white; }
  .trust-sub { font-size: 12.5px; color: rgba(255,255,255,0.7); font-weight: 600; margin-top: 2px; }

  .trust-link {
    flex-shrink: 0;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 13px;
    color: white;
    text-decoration: none;
    padding: 9px 16px;
    border-radius: 999px;
    background: rgba(255,255,255,0.16);
    white-space: nowrap;
    transition: background 0.2s ease;
  }
  .trust-link:hover { background: rgba(255,255,255,0.26); }

  /* ── SECTION HEADINGS ───────────────────────────────── */
  .section-head { text-align: center; margin-bottom: 50px; }
  .section-head h2 { font-size: clamp(26px, 4vw, 38px); margin-bottom: 14px; }
  .section-head p { color: rgba(255,255,255,0.78); font-size: 16px; max-width: 560px; margin: 0 auto; }

  /* ── GRID ───────────────────────────────────────────── */
  .grid-3 {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .feature-icon {
    width: 52px; height: 52px;
    border-radius: 16px;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 18px;
    font-size: 24px;
  }
  .icon-violet { background: hsl(var(--primary) / 0.18); }
  .icon-teal { background: hsl(var(--teal) / 0.18); }
  .icon-amber { background: hsl(var(--secondary) / 0.18); }

  .glass-card h3 { font-size: 19px; margin-bottom: 10px; color: white; }
  .glass-card p { color: rgba(255,255,255,0.78); font-size: 14.5px; }

  /* ── TICKER ROW ─────────────────────────────────────── */
  .ticker-row {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 20px;
  }
  .ticker-pill {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 13px;
    background: hsl(var(--secondary) / 0.18);
    color: hsl(var(--secondary));
    padding: 6px 14px;
    border-radius: 999px;
  }

  /* ── TERRITORY LIST ─────────────────────────────────── */
  .territory-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 14px;
    margin-top: 28px;
  }
  .territory-pill {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 14.5px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.18);
    padding: 10px 20px;
    border-radius: 999px;
  }

  /* ── AI SUPPORT ROW ─────────────────────────────────── */
  .support-row { display: flex; flex-direction: column; gap: 18px; }
  .support-item {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }
  .support-dot {
    flex-shrink: 0;
    width: 10px; height: 10px;
    border-radius: 50%;
    background: hsl(var(--secondary));
    margin-top: 8px;
  }
  .support-item h4 { font-size: 16px; color: white; margin-bottom: 4px; }
  .support-item p { color: rgba(255,255,255,0.78); font-size: 14.5px; }

  /* ── PARTNER STRIP ──────────────────────────────────── */
  .partner-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.18);
    border-radius: 999px;
    padding: 12px 24px;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 14.5px;
  }

  /* ── FINAL CTA ──────────────────────────────────────── */
  .final-cta { text-align: center; padding-bottom: 110px; }
  .final-cta h2 { font-size: clamp(28px, 4.5vw, 42px); margin-bottom: 16px; }
  .final-cta p { color: rgba(255,255,255,0.8); margin-bottom: 30px; }

  @media (max-width: 760px) {
    .grid-3 { grid-template-columns: 1fr; }
    .trust-strip { flex-wrap: wrap; }
    .section { padding: 64px 20px; }
  }
</style>
</head>
<body class="caribbean-bg">

  <!-- ============================================================
       SECTION 1 — HERO
       ============================================================ -->
  <section class="section hero">
    <div class="hero-logo">
      <div class="hero-bars">
        <div class="bar bar1"></div>
        <div class="bar bar2"></div>
        <div class="bar bar3"></div>
      </div>
      <div class="hero-wordmark">Finsight<span>Lite</span></div>
    </div>
    <h1>Money skills for the next generation of the Caribbean.</h1>
    <p>Finsight Lite helps young learners build real financial confidence through games, simulations, and lessons made for their world, not someone else's.</p>
    <a class="btn-primary" href="https://www.finsightlite.com" target="_blank" rel="noopener">
      Start Learning
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M5 12H19M19 12L13 6M19 12L13 18" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </a>
  </section>

  <!-- ============================================================
       SECTION 2 — ISTE TRUST STRIP
       ============================================================ -->
  <section class="section section-narrow">
    <div class="glass-card reveal trust-strip">
      <div class="trust-icon">
        <svg viewBox="0 0 200 220" width="26" height="28">
          <rect x="18" y="138" width="44" height="70" rx="22" fill="#A78BFA"/>
          <rect x="84" y="94" width="44" height="114" rx="22" fill="#5FD4E8"/>
          <rect x="150" y="40" width="44" height="168" rx="22" fill="url(#g1)"/>
          <circle cx="172" cy="24" r="20" fill="#F5A623"/>
          <defs><linearGradient id="g1" x1="172" y1="40" x2="172" y2="208"><stop offset="0%" stop-color="#EF7C5C"/><stop offset="100%" stop-color="#A78BFA"/></linearGradient></defs>
        </svg>
      </div>
      <div class="trust-text">
        <p class="trust-title">Listed in ISTE's Learning Technology Directory</p>
        <p class="trust-sub">Verified EdTech product &middot; ULTID P2BA4783BFDB4BC9EE</p>
      </div>
      <a class="trust-link" href="https://index.edsurge.com/" target="_blank" rel="noopener">View Profile</a>
    </div>
  </section>

  <!-- ============================================================
       SECTION 3 — WHAT IS FINSIGHT LITE
       ============================================================ -->
  <section class="section section-narrow">
    <div class="section-head reveal">
      <span class="eyebrow">What it is</span>
      <h2>Built for the Caribbean. Built for ages 12 to 17.</h2>
      <p>Instead of generic global finance content, every lesson, simulation, and example is grounded in Caribbean realities: real markets, real currencies, real decisions students will actually face.</p>
    </div>
  </section>

  <!-- ============================================================
       SECTION 4 — WHAT STUDENTS DO (3-COLUMN)
       ============================================================ -->
  <section class="section">
    <div class="section-head reveal">
      <span class="eyebrow">Inside the platform</span>
      <h2>What students actually do</h2>
    </div>
    <div class="grid-3">
      <div class="glass-card reveal">
        <div class="feature-icon icon-violet">📘</div>
        <h3>Learn</h3>
        <p>Nine core lessons across three foundational modules, covering saving, budgeting, and smart financial decision-making, built to meet students where they are.</p>
      </div>
      <div class="glass-card reveal">
        <div class="feature-icon icon-teal">🎮</div>
        <h3>Play</h3>
        <p>Seven hands-on money games, including Beat the Budget, that turn financial concepts into real practice instead of theory.</p>
      </div>
      <div class="glass-card reveal">
        <div class="feature-icon icon-amber">📈</div>
        <h3>Invest (virtually)</h3>
        <p>A live Investment Simulator using six real BISX-listed stocks, with six reading modules localized for each territory we serve.</p>
        <div class="ticker-row">
          <span class="ticker-pill">CBL</span>
          <span class="ticker-pill">FCL</span>
          <span class="ticker-pill">CAB</span>
          <span class="ticker-pill">DHS</span>
          <span class="ticker-pill">JSJ</span>
          <span class="ticker-pill">CHB</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================================================
       SECTION 5 — AI SUPPORT
       ============================================================ -->
  <section class="section section-narrow">
    <div class="glass-card reveal">
      <span class="eyebrow">Built in, not bolted on</span>
      <h2 style="font-size: 26px; margin-bottom: 24px;">Help when it's needed, not just when it's asked for</h2>
      <div class="support-row">
        <div class="support-item">
          <div class="support-dot"></div>
          <div>
            <h4>Money Guide</h4>
            <p>An AI chatbot students can ask real questions, anytime.</p>
          </div>
        </div>
        <div class="support-item">
          <div class="support-dot"></div>
          <div>
            <h4>AI Tutor</h4>
            <p>Automatically steps in when a student's quiz scores show they need extra support.</p>
          </div>
        </div>
        <div class="support-item">
          <div class="support-dot"></div>
          <div>
            <h4>AI-generated spending tips</h4>
            <p>Personalized nudges based on real activity, not generic advice.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================================================
       SECTION 6 — TERRITORIES
       ============================================================ -->
  <section class="section section-narrow">
    <div class="section-head reveal">
      <span class="eyebrow">Where we work</span>
      <h2>Made for five territories, not adapted from somewhere else.</h2>
    </div>
    <div class="territory-list reveal">
      <span class="territory-pill">🇧🇸 The Bahamas</span>
      <span class="territory-pill">🇧🇧 Barbados</span>
      <span class="territory-pill">🇯🇲 Jamaica</span>
      <span class="territory-pill">🇹🇹 Trinidad and Tobago</span>
      <span class="territory-pill">🌴 Eastern Caribbean</span>
    </div>
  </section>

  <!-- ============================================================
       SECTION 7 — FOR TEACHERS
       ============================================================ -->
  <section class="section section-narrow">
    <div class="glass-card-light reveal">
      <span class="eyebrow" style="color: hsl(var(--primary));">For teachers</span>
      <h3 style="font-size: 22px;">Built to fit your classroom, not replace it.</h3>
      <p style="color: hsl(var(--ink) / 0.75); margin-top: 10px;">Teachers can link external YouTube videos or upload their own content for their specific classes, keeping lessons connected to what's already happening in the room.</p>
    </div>
  </section>

  <!-- ============================================================
       SECTION 8 — PARTNERS
       ============================================================ -->
  <section class="section section-narrow">
    <div class="section-head reveal">
      <span class="eyebrow">Recognized in the field</span>
      <h2 style="font-size: 24px;">Trusted by educators, recognized by the index.</h2>
    </div>
    <div style="display: flex; justify-content: center; flex-wrap: wrap; gap: 14px;" class="reveal">
      <span class="partner-badge">🤝 The Financial Academy</span>
      <span class="partner-badge">✅ ISTE Learning Technology Directory</span>
    </div>
  </section>

  <!-- ============================================================
       SECTION 9 — FINAL CTA
       ============================================================ -->
  <section class="section final-cta">
    <h2 class="reveal">Ready to see it in action?</h2>
    <p class="reveal">Join students across the Caribbean building real financial confidence.</p>
    <a class="btn-primary reveal" href="https://www.finsightlite.com" target="_blank" rel="noopener">
      Visit Finsight Lite
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M5 12H19M19 12L13 6M19 12L13 18" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </a>
  </section>

  <script>
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15 });
    reveals.forEach(el => observer.observe(el));
  </script>

</body>
</html>