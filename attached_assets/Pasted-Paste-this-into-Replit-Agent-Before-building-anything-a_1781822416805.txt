Paste this into Replit Agent:

Before building anything, audit the codebase and answer each question below. Then for each feature, follow the matching instruction.

AUDIT FIRST — check for each of these:

Is there any existing email sending setup? (Look for Resend, SendGrid, Nodemailer, or any SMTP configuration in the codebase or .env variables)
Is there any existing PDF generation? (Look for puppeteer, jsPDF, pdfkit, or similar)
Is there a cron job or scheduled task system already set up? (Look for node-cron, Bull, or any scheduler)
Is there an existing Cloudflare configuration or is the app served directly from Railway?
Does the org dashboard already have any AI-generated text or Anthropic API calls on the frontend?

Print the answer to each question before writing any code.

FEATURE 1 — AI org summary on the dashboard
If an Anthropic API call already exists on the org dashboard: add the summary to that existing call, do not create a second one.
If no Anthropic call exists on the org dashboard yet: add a new server-side API endpoint at GET /api/org/summary that pulls the logged-in org's stats (total students, active students last 30 days, average XP, lesson completion rate) and sends them to the Anthropic API with this system prompt:
"You are a helpful assistant for Finsight Lite, a Caribbean financial literacy platform for youth ages 12–17. Write a 2–3 sentence plain-English summary of this organization's student engagement this month. Be specific with the numbers. Keep the tone warm and encouraging. Do not use em dashes."
Return the summary as a string. On the dashboard, display it inside a glass-card-teal panel at the top of the page with the label "This Month at a Glance" in font-display font-bold text-white. Cache the response for 24 hours per org so it does not call the API on every page load.

FEATURE 2 — PDF export of the org dashboard
If PDF generation is already set up: use the existing library to generate the export.
If no PDF library exists: install pdfkit and create a server-side endpoint at GET /api/org/report/pdf that generates a single-page PDF containing:

Organization name and territory at the top
Report period (current month and year)
The following metrics in a clean layout: total students, active students, average XP, lesson completion rate, top 3 money games by session count
A simple student table: name, total XP, lessons completed, last active — maximum 25 rows
Footer: "Generated by Finsight Lite · www.finsightlite.com · Finsight Limited"

On the org dashboard, add a button labeled "Export Report" that hits this endpoint and triggers a file download. Style the button using the existing btn-primary class.
Do not use dummy data in the PDF. Pull the same live queries already wired on the dashboard. If a query is still on a TODO, omit that metric from the PDF and note it with a comment.

FEATURE 3 — Weekly automated email to org admins
If an email provider is already configured: use it. If not: install resend (npm package) and add a RESEND_API_KEY placeholder to the .env file with a comment saying "Get free API key at resend.com".
If a cron/scheduler already exists: add the weekly job to it. If not: install node-cron and create a scheduler file at server/scheduler.ts that runs every Monday at 8:00 AM UTC.
The weekly job should:

Query every organization that has at least 1 active student in the last 7 days
For each org, fetch: org name, total students, active students last 7 days, average XP, top game this week
Send one email to the org admin's email address with a subject line: "Your Finsight Lite weekly update — [Org Name]"
Email body should be plain and readable, not HTML-heavy. Include the stats, one sentence of encouragement, and a link to their dashboard at www.finsightlite.com/org/dashboard
Do not send an email if the org had zero activity that week

Register the scheduler in the main server entry point so it starts with the app.

Constraints:

Do not change any existing routes, auth flows, or UI outside of the org dashboard additions above.
All new environment variables get a placeholder value and a comment explaining where to get the real key.
After completing all three features, print every file created or modified and flag any feature that could not be fully completed and why.